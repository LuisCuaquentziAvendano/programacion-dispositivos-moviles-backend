# Requirements
- Docker
- Node.js

# Steps to run the project
1. Create the `.env` file (check `.env.example`).
2. Open the terminal and run the command `npm install` to install dependencies.
3. Run the command `npm run dev` to start the app.
4. Open other terminal and run the command `npm run db:studio` to open the database visualizer.
